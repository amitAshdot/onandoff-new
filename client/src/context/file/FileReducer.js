import {
    ADD_WEBSITE,
    DELETE_WEBSITE,
    SET_CURRENT,
    CLEAR_CURRENT,
    UPDATE_WEBSITE,
    FILTER_WEBSITE,
    CLEAR_FILTER,
    WEBSITE_ERROR,
    GET_WEBSITE,
    CLEAR_WEBSITES

} from '../type';

export default (state, action) => {
    switch (action.type) {

        
        default:
            return state;
    }
}